{
  "name": "radix-vue-clone",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm  -r --filter=radix-vue build",
    "pg":"pnpm build && pnpm -r --filter=playground-vue dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.20"
  }
}